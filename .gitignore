node_modules
.next
out
build
.env
.env*.local
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo

# business secrets — NEVER commit (public repo)
# cost prices (ราคาช่าง) live in products.json; price sheets show ราคาช่าง too
data/products.json
data/sheets/
# supplier price-list photos (CMIT etc) — member-only pricing, reference archive only
cmit/

# staff-uploaded sales report photos (local dev only — prod uses a Docker volume)
public/uploads/
